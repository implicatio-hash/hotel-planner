{
  "name": "hotel-planner-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions – Gemini AI proxy for Hotel Planner",
  "main": "index.js",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-functions": "^4.9.0",
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "firebase-admin": "^12.0.0"
  }
}
